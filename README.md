# Dynamic_DNS_Resolver_Selection

This project implements a Dynamic DNS Resolver Selection system that selects the most suitable DNS resolver based on predefined criteria(risk score). The goal is to improve DNS query performance by dynamically choosing the best resolver instead of relying on a fixed one. 